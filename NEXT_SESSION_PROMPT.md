# きずなbaton 次セッション開始プロンプト（2026-06-04 更新 / v44 反映）

このまま下記をコピペして、新しいClaudeセッションのキックオフに使ってください。

> 🟦 **次セッションの主担当＝アプリ開発**。セッション開始時に、まず下記「アプリ開発タスク」一覧を提示してオーナーに着手タスクを選ばせること。SNS/Meta広告は副次（やる場合はオーナーから明示指示があったとき）。
> 着手前のルール（CLAUDE.md）: 大きめの変更は **plan mode で計画提示→承認後に実装**。動作確認は `preview_start`+`preview_screenshot`。完了したら2ファイル（`shukatsu-prototype.html`/`sw.js`）のみ commit→push→本番curl確認→Desktop(`アプリ作成/Claude/`)上書き→引き継ぎ3点更新。

---

## 🟦 アプリ開発タスク（次セッションでまず提示する）

**直近完了（参考）**: v44 = 招待データ(invitations)のlocalStorage永続化（S8-3課題1）。リロード/SW更新で招待・受諾家族が消える不具合を修正。デプロイ済 `5e09664`・本番curl確認済。/ v43 = ①老眼モード(U1) ②設定歯車を上部バー右上へ移動。デプロイ済 `40f44c8`。

### 着手候補（オーナーに選ばせる）
1. **v43 実機確認**（最小工数）— 既存利用者の端末でSW更新後、上部の歯車⚙＋「文字を大きく」トグルが反映されるか。
2. **設定歯車の完全固定(sticky)**（小）— 現状は上部バーがスクロールで流れる。`.user-bar` を sticky 化し、下にスクロールしても歯車が常時見えるようにする（高齢者UXさらに改善）。
3. **老眼モードの拡大幅調整**（小）— +2px→+3〜4px などオーナー好みに。`:root.large` の値だけ調整。
4. **第4スプリント候補C「もしもの時」2段階発動UI**（規模大・未着手）— 相続判断モード/完全開示モードの発動フロー画面（CLAUDE.md「2段階発動」準拠）。要件定義から。
5. **S8-3 localStorage 永続化の残課題**（課題1=invitations永続化は v44 で完了）— 残: 課題2 スキーママイグレーション未実装（`DATA_KEY` が `:v20` 固定・キー名変更で旧データ孤児化）/ 課題3 新規mutationでの `persistState()` 呼び忘れリスク（保存を一元化する設計検討）/ 課題4 iOS Safari 7日eviction（コード対処困難・JSONバックアップ案内で緩和）。いずれも実害小のため優先度は低め。
6. **F1 アプリ内レビュー誘導 / F2 アプリ内フィードバック / F3 プラン価格戦略(RevenueCat前提)**（未着手）。

> **見送り中（オーナー判断で凍結）**: N3 各支払日のカレンダー表示（規模大）。再開はオーナー判断。

---

## 🟢 副次タスク（マーケ：オーナー指示時のみ）

### SNS T2投稿（X + IGカルーセル2・2026-06-05）— 未実施
- `marketing/social-post-queue.md` の **T2（06-05）** の X投稿文を使って投稿。T2はIG投稿日（カルーセル2「カード番号は、預かりません」）。**IG画像5枚アップロードのみオーナー手動**。投稿後 `marketing/social-posting-log.md` の T2 行にURL記録。禁止表現チェック必須。
- 起動合言葉: **「今日のSNS投稿実行」** → `social-post-queue.md` の次の未投稿T番号を投稿。
- > T0（06-03）/ T1（06-04・Xのみ）実施済。T1 X URL: https://x.com/kizuna_baton/status/2062348575921815723

### Meta広告の1週間後チェック（2026-06-10頃）
- 直近（2026-06-04・配信2日目）: A案 CTR **2.11%** / CPC **¥57** / インプレ **283** / リーチ **280** / 消化 **¥336** / クリック約6。Formspree登録 **0件**。好シグナルだが配信2日で早期、確定判断は06-10 / 最終06-17。
- 手順: ①`marketing/monitoring-log.md` の閾値再確認 ②**オーナーがMeta Ads Manager実機を読み上げ**（Claude単独取得不可）③Formspree(`formspree.io/f/xredoben`)登録数記入 ④閾値照合→前進策提案 ⑤実機変更は全てオーナー承認後。

---

## 🟢 SNS日次投稿状況

| T番号 | 日付 | X | Instagram |
|---|---|---|---|
| T0 | 2026-06-03 | ✅ 完了 | ✅ 完了（カルーセル1） |
| T1 | 2026-06-04 | ✅ 完了 | なし |
| T2 | 2026-06-05 | **未投稿→次回投稿予定**（X文面は social-post-queue.md の T2） | ✅ 完了（カルーセル2 https://www.instagram.com/p/DZKJVlIk82n/） |
| T3 | 2026-06-06 | **未実施** | なし |

- X は毎日。IG は T0/T2/T4/T7/T9/T11 のみ。
- **起動: 「今日のSNS投稿実行」** と一声 → `social-post-queue.md` から次の未投稿T番号を投稿。
- IG投稿日は**画像5枚アップロードのみオーナー手動**（file_upload のホストパス拒否は既知・修正不可）。

---

## まず読む（順番厳守）

1. `kizuna-baton/CLAUDE.md`（アプリ作業ルール・データ範囲・2段階発動・UI規約）
2. `kizuna-baton/HANDOVER.md`（最新の実装履歴・v43まで）
3. `kizuna-baton/handover-doc.md`（主要機能 vNN 詳細・残タスク一覧「アプリ開発の真の残タスク」）
4. 副次（マーケをやる場合のみ）: `marketing/monitoring-log.md`（Meta KPI）/ `marketing/social-posting-log.md`（SNS進捗）/ `marketing/social-post-queue.md`（投稿文面）

読んだら、`HANDOVER.md`・`handover-doc.md` と実ファイル（`shukatsu-prototype.html`）を突き合わせて事実確認すること（ユーザー貼付の完了報告を鵜呑みにしない）。**開始時にまず「アプリ開発タスク」一覧を提示する。**

---

## 現在の状態（2026-06-04 時点）

- **Meta広告A案**: 2026-06-03〜06-17の2週間テスト中（¥350/日・A案のみ）。配信2日目で良好シグナル。
- **SNS**: X/Instagram @kizuna_baton・T0（06-03）/ T1（06-04・Xのみ）完了。次はT2（06-05・X+IGカルーセル2）。
- **Formspree**: 0件（2026-06-04 確認）。広告クリック約6件あるがLPからの登録に至っていない段階。
- **アプリ**: **v44デプロイ済み（`5e09664`）= 招待データ(invitations)のlocalStorage永続化（S8-3課題1）**。本番curl確認済(title v44 / CACHE v44)・Desktop上書き済・preview検証済(A〜E)。次セッションで実機SW更新後の招待永続確認を推奨。/ v42 = ベータ版5件制限（CONTRACT_BETA_MAX=5）実機確認済。
- **v43 = デプロイ済 `40f44c8`・本番curl確認済**（2変更を同梱）: ①老眼モード(U1)＝設定モーダルに「文字を大きく」トグル（`:root.large` で主要テキスト+2px・ダークモードと同型）②設定歯車⚙を契約リスト最下部から**上部バー右上**（家族管理アイコン隣）へ移動（高齢者UX改善・スクロール不要に）。Desktop（`アプリ作成/Claude/`）へ上書き済。次セッションで実機SW更新後の反映確認推奨。N3カレンダーは引き続き見送り中。
- **作業ツリー**: `shukatsu-prototype.html`/`sw.js` はコミット済。`marketing/`配下は従来どおりuntracked（gitignore外の運用ファイル）。

---

## ⚠️ Instagram投稿の確定知見（IG投稿日 T2/T4/T7/T9/T11 で必ず参照）

- `file_upload` ツールはホストパスを拒否する（既知・修正不可）→ **画像5枚アップロードのみオーナー手動**。
- 操作は `javascript_tool` 使用。React `div[role=button]` は pointerdown→mousedown→pointerup→mouseup→click のイベント列で発火。
- キャプション欄（contenteditable）は `focus()` + `execCommand('insertText')` + Enter で改行。
- キャプション文面は `social-post-queue.md` / `instagram-carousel-production.md` の固定文面を改変禁止で使う。
- **投稿前に必ず全スライドを確認**（クロップ画面の送りは不安定なので「次へ」で編集ステップに進み右矢印で全5枚チェック）。過去に別カルーセルの画像が誤って下書きに入っていた事例あり。1枚目のSLIDE_LABELと見出しが目的のカルーセルと一致するか毎回照合する。
- キャプション改行は execCommand('insertText') では潰れる。Cmd+A→Delete で一度クリア後、computer `type`＋`key Return Return` で段落ごとに入力すると改行が反映される。

---

## Chrome操作の既知制約

- computer-use は Chrome = read tier（screenshot のみ・クリック/入力不可）。
- Claude-in-Chrome MCP のタブがバックグラウンドウィンドウにある場合、Meta SPA が描画されない。
- **Meta Ads Manager の実数値はオーナー読み上げが最確実**。

---

## 禁止表現（SNS投稿前に必ずチェック）

- 死亡判定 / 生存確認 / 死後すぐ解約 / 家族が代わりに手続き
- 相続放棄の判断をアプリが助ける等の法的判断示唆
- カード番号・口座番号・暗証番号を預かるように読める表現
- 「必ず」「完全に」「安心です」等の過度な保証
- 恐怖をあおるだけのコピー

---

## 次アクション候補（アプリ開発優先で提示する）

1. **v43 実機確認**（最小工数・上部歯車＋文字拡大トグルの反映チェック）
2. **設定歯車の完全固定(sticky)** または **老眼モード拡大幅調整**（どちらも小規模UX改善）
3. **第4スプリント候補C「もしもの時」2段階発動UI の要件定義**（規模大・本格着手）

> マーケ（SNS T2 / Meta広告チェック）はオーナーから明示指示があった場合のみ。
